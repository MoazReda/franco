# Franco Translator — Python dependencies
# Python 3.11+ required
# Install:  pip install -r requirements.txt

# ── Data collection & parsing ──────────────────────────────────────────
google-api-python-client>=2.150,<3.0     # YouTube Data API v3
requests>=2.31,<3.0                       # Reddit public API
python-dotenv>=1.0,<2.0                   # Load API keys from .env

# ── Data processing ────────────────────────────────────────────────────
pandas>=2.2,<3.0
numpy>=1.26,<3.0
openpyxl>=3.1,<4.0                        # Excel I/O for annotation sheets
gspread>=6.0,<7.0                         # Google Sheets (optional)

# ── EDA & visualization ────────────────────────────────────────────────
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0

# ── NLP / ML (Phase 2+) ────────────────────────────────────────────────
# Note: for GPU support install torch with CUDA build separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2,<3.0
transformers>=4.40,<5.0
sentencepiece>=0.2,<1.0                   # Required by mT5 / AraT5 tokenizers
accelerate>=0.30,<2.0                     # HF training optimizer
datasets>=2.18,<4.0                       # HF datasets loader
evaluate>=0.4,<1.0                        # HF evaluation framework
sacrebleu>=2.4,<3.0                       # BLEU / chrF metrics
scikit-learn>=1.4,<2.0                    # Train/val/test split + utilities

# ── Notebooks ──────────────────────────────────────────────────────────
jupyter>=1.0,<2.0
ipykernel>=6.29,<7.0
tqdm>=4.66,<5.0                           # Progress bars

# ── Dev tooling ────────────────────────────────────────────────────────
pytest>=8.0,<9.0
ruff>=0.4,<1.0                            # Lint + format
